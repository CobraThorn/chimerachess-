# Copy to Netlify/Vercel environment variables before production build.
# Replace with your real custom domains after DNS is live.

VITE_CHIMERA_API_URL=https://api.chimerachess.co.uk
# VITE_OPENAI_API_KEY=sk-...

# Render / Railway (server only — not Vite):
# CHIMERA_CORS_ORIGIN=https://chimerachess.co.uk
# CHIMERA_DATA_DIR=/app/server/data
