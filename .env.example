# Frontend (Vite) — set before npm run build
# Custom domain example (see docs/DEPLOY-CUSTOM-DOMAIN.md):
VITE_CHIMERA_API_URL=https://api.chimerachess.co.uk
# Or Render default until DNS is ready:
# VITE_CHIMERA_API_URL=https://chimera-data-api.onrender.com

# Optional: OpenAI for opening coach
# VITE_OPENAI_API_KEY=sk-...

# --- Server only (Railway / Render dashboard) ---
# PORT is set automatically by the host

# Persist data on a mounted volume (Railway)
# CHIMERA_DATA_DIR=/app/server/data

# CORS (Render/Railway): must match your app URL exactly
# CHIMERA_CORS_ORIGIN=https://chimerachess.co.uk
